
  # Security Management Application

  This is a code bundle for Security Management Application. The original project is available at https://www.figma.com/design/2NwbG7lcdne36VuYQ76KoL/Security-Management-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  